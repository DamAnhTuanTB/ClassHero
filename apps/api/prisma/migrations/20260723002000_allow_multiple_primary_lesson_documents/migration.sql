-- A lesson may have multiple source page-range foundation documents and
-- multiple directly uploaded foundation documents.

DROP INDEX IF EXISTS "lesson_documents_one_active_primary_per_lesson_idx";
