# M5. Embedding + RAG retrieval

Chi tiết phạm vi, Không làm và Done khi cho các subtask M5.x.

---

## M5: Embedding + RAG retrieval

Trạng thái milestone: **Done ngày 2026-07-27**.

- `M5.1`: OpenAI embedding chạy qua `AiProvider`, env bị fail-fast nếu dimension không khớp `vector(1536)`, response provider được kiểm model/dimension/số vector/giá trị hữu hạn.
- `M5.2`: durable embedding job + `ai_generations`, batch/retry/final error, token usage/latency và trạng thái document đầy đủ; clone lộ trình cá nhân tái sử dụng vector hợp lệ và enqueue idempotent phần còn thiếu.
- `M5.3`: pgvector cosine search chỉ đọc tài liệu active `READY` trong đúng lesson và đúng provider/model/dimension; PostgreSQL integration test chứng minh không leak giữa lesson.
- `M5.4`: keyword fallback cho công thức/ký hiệu/đơn vị, merge + deduplicate ổn định và không bao giờ vượt context token budget.
- Migration bù `20260727001000_restore_embedding_hnsw_index` bảo đảm HNSW cosine index tồn tại; OpenAI live smoke đã xác nhận vector 1.536 chiều.

### M5.1: AiProvider abstraction cho embedding

Mode: Worker/Integration

Mục tiêu:

- Có lớp AI provider abstraction nền.

Phạm vi:

- `AiProvider` interface.
- OpenAI embedding provider.
- Config model/env validation.
- Không gọi Gemini cho embedding trong MVP trừ khi owner yêu cầu.

Done khi:

- Code gọi embedding thông qua abstraction.
- Không trộn provider/model trong cùng vector space.

### M5.2: Embedding worker và lưu pgvector

Mode: Worker/Integration

Mục tiêu:

- Tạo embedding cho chunks.

Phạm vi:

- Embedding job.
- Batch embedding nếu phù hợp.
- Lưu vector vào `document_chunks`.
- Lưu provider/model/dimensions/token usage nếu có.

Done khi:

- Chunk có vector.
- Job retry/error hoạt động.

### M5.3: RetrievalService vector search theo lesson

Mode: Worker/Integration

Mục tiêu:

- Tìm context theo đúng lesson.
- Chapter chỉ là nhóm tổng quan nên retrieval không filter theo chapter ở MVP.

Phạm vi:

- Raw SQL pgvector search.
- Filter theo `lesson_id`, provider, model, dimensions.
- Không trả chunk lesson khác.
- Unit/integration test retrieval cơ bản.

Done khi:

- `retrieveContext(lessonId, query)` hoạt động.
- Test chứng minh không leak context giữa lesson.

### M5.4: Hybrid search cho công thức/ký hiệu

Mode: Worker/Integration

Mục tiêu:

- Bổ sung keyword fallback cho Toán/Lý/Hóa.

Phạm vi:

- Keyword search trong cùng lesson.
- Bắt LaTeX/ký hiệu/đơn vị/tên định luật/tên phản ứng.
- Merge kết quả vector + keyword.

Done khi:

- Retrieval có fallback keyword.
- Không vượt token budget context.

---
