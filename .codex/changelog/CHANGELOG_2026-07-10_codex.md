- 2026-07-10: Nối auth UI với API thật cho đăng nhập, đăng ký học sinh/phụ huynh, quên/đặt lại mật khẩu, bổ sung API client/session store/TanStack Query provider, chỉnh validation DTO và reset-password flow phía API, đồng thời chuẩn hóa toast global theo trạng thái với countdown và cập nhật docs/context/skill cho workflow nối API.
- 2026-07-10: Thêm M3.1 learning path API với public published list/detail tối thiểu, admin CRUD/publish/soft-delete có RBAC, validation, pagination và audit log, đồng thời cập nhật contract/context/coverage và ghi nhớ rule UI hỗ trợ theme sáng/tối về sau.
- 2026-07-10: Chuyển API dev runtime sang build watch có decorator metadata đầy đủ, bỏ validation pipe thủ công ở các controller auth/learning path để dùng global validation chuẩn và giữ DTO validation ổn định khi chạy dev.
- 2026-07-10: Thêm M3.2 admin lesson API cho quản lý buổi học trong lộ trình với CRUD/publish, RBAC, validation order/video/completion score, cập nhật tổng số buổi và audit log, đồng thời làm rõ API contract, coverage và context/code index cho bước public listing tiếp theo.
- 2026-07-10: Tổ chức lại cấu trúc frontend/backend theo layer rõ ràng, chuyển auth/public routes và admin courses UI sang component/hook/schema/data tách riêng, chuẩn hóa alias import `@/...` và `#api/...`, bổ sung Next layout/toaster shared, gom backend module theo controllers/services/selectors/serializers/utils/types và đưa HTTP exception cùng Prisma error mapper vào `common/errors` để tái sử dụng thống nhất.
- 2026-07-10: Bổ sung contract cấu trúc source code cho front-end/back-end, cập nhật docs map, architecture, UI docs, implementation plan, ADR, context, prompts và các skill chính để bắt buộc chọn đúng shared/feature/domain layer, tái sử dụng pattern đã duyệt, dùng alias import và tránh gom component/helper/service vào file hoặc module phẳng.
- 2026-07-10: Mở rộng admin course management thành flow quản trị lộ trình nhiều màn với danh sách, chi tiết chương/buổi học, modal form, upload cover mock, thùng rác/khôi phục và query hook mock API, đồng thời tách service token/public learning path ở API và cập nhật docs/skills/pattern để chuẩn hóa form, modal, upload, state và code-pattern workflow.
