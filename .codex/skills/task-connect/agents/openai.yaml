interface:
  display_name: "Task Connect Runner"
  short_description: "Plan or connect finished UI to APIs"
  default_prompt: "Use $task-connect to run /task-connect plan M7.1 for an approval-gated connection plan, or /task-connect M7.1 to connect UI to APIs."
