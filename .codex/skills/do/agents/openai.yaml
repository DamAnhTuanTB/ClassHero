interface:
  display_name: "Do Runner"
  short_description: "Execute or plan from the latest approved direction"
  default_prompt: "Use $do after a reviewed plan or next-task recommendation to run /do, optionally commit finished work first, then execute or plan the next approved work."
